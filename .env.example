# ========== 前端环境变量（Vite 自动读取，VITE_ 前缀必须） ==========

# 后端 API 地址（同域部署时填 /，跨域填完整地址如 http://localhost:3000/）
VITE_API_BASE=/

# Pixiv API 地址（可选，留空则禁用 Pixiv 功能）
VITE_PIXIV_API_BASE=

# ========== 以下变量供 server/app.js 使用（通过 PM2 传入） ==========
# 数据源站点地址（必填）
SOURCE_BASE=https://your-source-site.com
